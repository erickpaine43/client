# Database Configuration
NILEDB_USER=niledb_user
NILEDB_PASSWORD=niledb_password
NILEDB_API_URL=https://us-west-2.api.thenile.dev/v2/databases/<database_id>
NILEDB_POSTGRES_URL=postgres://us-west-2.db.thenile.dev:5432/<database_name>

# Cloudflare Deployment Configuration
# These are set via wrangler secrets and .dev.vars
# CLOUDFLARE_API_TOKEN=<your_api_token>       # Set in GitHub Secrets
# CLOUDFLARE_ACCOUNT_ID=<your_account_id>     # Set in GitHub Secrets

# Next.js Environment (set in .dev.vars)
NEXTJS_ENV=development # For development, production for deployment

# Optional: R2 Bucket for caching (uncomment if you create an R2 bucket)
# NEXT_INC_CACHE_R2_BUCKET=<your_r2_bucket_name>
